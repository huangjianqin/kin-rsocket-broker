server:
  port: 9100

spring:
  netty:
    leak-detection: paranoid

### management
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always